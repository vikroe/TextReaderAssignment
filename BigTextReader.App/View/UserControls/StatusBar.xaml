<UserControl x:Class="BigTextReader.App.View.UserControls.StatusBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BigTextReader.App.View.UserControls"
             mc:Ignorable="d" 
             Height="24" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="24" />
        </Grid.RowDefinitions>
        <StatusBar>
            <StatusBarItem>
                <TextBlock Text="{Binding LineCount, StringFormat={}{0:N0} lines}" />
            </StatusBarItem>
            <StatusBarItem>
                <ProgressBar Width="160" Height="14" Minimum="0" Maximum="1" 
                             Value="{Binding Progress, Mode=OneWay}"
                             Visibility="{Binding Busy, Converter={StaticResource BoolToVis}}"
                             IsIndeterminate="{Binding ProgressIndeterminate}" />
            </StatusBarItem>
            <StatusBarItem>
                <TextBlock Text="{Binding StatusText}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
